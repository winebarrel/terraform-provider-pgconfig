coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: 90%
        threshold: 0%

comment:
  layout: "reach, diff, files"
  require_changes: true
